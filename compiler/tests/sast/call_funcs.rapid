//too(); // one statement
int n = foo(); // this is vdecl stmnt with a funcall expr.

boo();

func too(){
}

// now w/ return type
func foo() int {
    int x;
	(x, int y, int z) = boo(); //Also one statement, boo not an expr.
	return 5;
}

// now w/ return type tuple
func boo() int, int, int {
	int a = 2;
	int b = 3;
	return a, b, 4;
}
