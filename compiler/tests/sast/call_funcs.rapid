too();
int n = foo();
func too(){
}

// now w/ return type
func foo() int {
	(x, int y, int z) = boo();
	return 5;
}

// now w/ return type tuple
func boo() int, int, int {
	int a = 2;
	int b = 3;
	return a, b, 4;
}
