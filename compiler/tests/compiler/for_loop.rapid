
list<int> my_list = [1, 13, 5];

for (int num in my_list) {
    int n = 7;

    Printf("%d ", num);
    Printf("%d ", n);
}

for (int num in [5, 6, 7]) {
    int n = 7;
    Printf("%d ", num);
    Printf("%d ", n);
}
