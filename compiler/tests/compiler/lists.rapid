list<string> x;
list<int> y = [];
list<int> z = [1, 2, 3];

list<list<int>> a = [
  z,
  [1, 2]
];

list<int> b = [1,2];

func foo(list<int> q)
{
	Println(q[0]);
}

foo(b);

Println(a[0][0]);
Println(a[1][1]);
